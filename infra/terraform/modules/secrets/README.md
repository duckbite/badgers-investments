## Module: secrets

Creates an AWS Secrets Manager secret containing JSON configuration consumed by Lambda (and other compute) via Terraform-injected env or runtime reads.

Stored keys:

- `COOKIE_SECRET`
