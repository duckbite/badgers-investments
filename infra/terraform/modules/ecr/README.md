## Module: ecr

Creates ECR repositories for the deployable containers:

- `web`
- `api`
- `worker`

Repositories are configured as immutable and have a basic lifecycle policy to keep the most recent images.

